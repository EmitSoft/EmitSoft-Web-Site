name: Teste CI - Echo iformacap

on:
  push:
    branches:
      - master  # ou main, se for o nome da sua branch principal

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout do código
        uses: actions/checkout@v4

      - name: Rodar echo iformacap
        run: echo "Rodando iformacap..."
